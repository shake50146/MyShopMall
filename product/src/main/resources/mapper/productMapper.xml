<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shake.product.mapper.productMapper">

    <!-- public ProductEntity selectProductByName(@Param("name") String name); -->
    <select id="selectProductByName" resultType="com.shake.product.model.entity.ProductEntity">
        select * from product where name = #{name}
    </select>

</mapper>